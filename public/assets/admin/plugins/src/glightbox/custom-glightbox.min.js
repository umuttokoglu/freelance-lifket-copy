// Default
const defaultGlightbox = GLightbox({
    selector: '.defaultGlightbox',
    loop: true,
    width: "90vw",
    height: "90vh"
});

// With Description
const withDescriptionGlightbox = GLightbox({
    selector: '.withDescriptionGlightbox'
});

// Iframe
const iframeGlightbox = GLightbox({
    selector: '.iframeGlightbox'
});
